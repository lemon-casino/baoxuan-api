const dateUtil = require("../utils/dateUtil")

describe("", ()=>{
    it("", ()=>{
        const dateStr = dateUtil.format2Str(new Date())
        console.log(dateStr)
        console.log(dateUtil.startOfToday())
        console.log(dateUtil.endOfToday())
    })
})